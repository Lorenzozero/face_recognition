# face_recognition-ng requirements
# Compatibile con Python 3.9+, nessuna compilazione C++ richiesta

# Core — riconoscimento facciale
insightface>=0.7.3
onnxruntime>=1.16.0          # CPU — sostituire con onnxruntime-gpu per CUDA
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# REST API + WebSocket
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
websockets>=12.0

# HTTP client (OSINT engine)
httpx>=0.27.0

# Database volti noti + OSINT
# sqlite3 è incluso nella stdlib Python — nessuna dipendenza aggiuntiva
# rate_limiter.py usa solo stdlib (threading, time) — nessuna dipendenza aggiuntiva

# Report PDF
reportlab>=4.1.0
matplotlib>=3.8.0

# Opzionale — OSINT username discovery su 3000+ siti
# pip install maigret
